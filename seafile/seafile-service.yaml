apiVersion: v1
kind: Service
metadata:
  name: seafile
  namespace: seafile
spec:
  selector:
    app: seafile
  type: ClusterIP
  ports:
    - name: http-seahub  
      protocol: TCP
      port: 8000
      targetPort: 80
    # - name: http-fileserver
    #   protocol: TCP
    #   port: 8082
    #   targetPort: 8082
    