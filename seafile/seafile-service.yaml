apiVersion: v1
kind: Service
metadata:
  labels:
    app: seafile
  name: seafile
  namespace: seafile
spec:
  selector:
    app: seafile
  ports:
    - protocol: TCP
      port: 31313
      targetPort: 80
  type: LoadBalancer
